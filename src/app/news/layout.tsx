export const metadata = {
  title: 'News',
  description: 'Latest updates, publications, and milestones.',
}

export default function NewsLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
